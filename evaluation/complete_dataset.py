data = {
    "question": [
        # OpenCV
        "Which function in OpenCV should be used for edge detection using the Canny method?",
        "What is the purpose of the cv2.findContours function?",
        "How do you perform adaptive thresholding in OpenCV?",
        "What is the difference between a Gaussian filter and a median filter?",
        "How do you convert the color space from BGR to Gray or HSV in OpenCV?",
        "What is the SIFT (Scale-Invariant Feature Transform) algorithm used for?",
        "How do you perform a dilation operation on a binary image?",
        "How do you calculate an image histogram in OpenCV?",
        "What does the cv2.warpAffine function do?",
        "How do you implement object tracking using the MeanShift method?",
        # Open3D
        "How do you load a point cloud file (e.g., .pcd or .ply) in the Open3D library?",
        "How do you perform point cloud simplification using the Voxel Downsampling method?",
        "What is the purpose of the ICP (Iterative Closest Point) algorithm in Open3D?",
        "How do you calculate surface normals for points in a cloud?",
        "How do you remove noise from a point cloud using the Statistical Outlier Removal method in Open3D?",
        "How do you visualize a point cloud object in an interactive window?",
        "What is RANSAC in the context of plane segmentation in Open3D?",
        "How do you transform a point cloud using a 4x4 transformation matrix?",
        "How do you create a triangle mesh from a point cloud using the Ball Pivoting method in Open3D?",
        "How do you extract only the points located inside a specific Bounding Box?",
        # PCL
        "How does the PassThrough filter work in the PCL library?",
        "What is the purpose of the pcl::VoxelGrid class?",
        "How do you implement Euclidean Cluster Extraction segmentation?",
        "What is the FPFH (Fast Point Feature Histograms) descriptor?",
        "How do you remove outliers using RadiusOutlierRemoval?",
        "What is the purpose of the pcl::SACSegmentation module in PCL?",
        "How do you merge two point clouds into one in PCL (C++)?",
        "What does the Moving Least Squares (MLS) algorithm do in PCL?",
        "How do you define a KDTree for fast neighbor searching in PCL?",
        "What is the purpose of the NARF (Normally Aligned Radial Feature) descriptor?",
    ],
    "ground_truth": [
        # OpenCV
        "You should use the cv2.Canny() function, which takes the input image and two thresholds (threshold1 and threshold2) for the hysteresis procedure.",
        "The function is used to detect contours in a binary image. It returns a list of contours and their hierarchy, allowing for shape and object analysis.",
        "The cv2.adaptiveThreshold() function is used, which allows for calculating the threshold for small regions of the image, making it effective under varying lighting conditions.",
        "A Gaussian filter reduces noise by weighted averaging of neighboring pixels, while a median filter replaces a pixel with the median of its neighborhood, which is better for removing salt-and-pepper noise.",
        "You should use the cv2.cvtColor(image, flag) function, where the flag is, for example, cv2.COLOR_BGR2GRAY or cv2.COLOR_BGR2HSV.",
        "SIFT is used for detecting and describing local image features that are invariant to scale changes, rotation, and illumination changes.",
        "The cv2.dilate() function is used, which expands the bright areas of the image (objects) at the expense of darker ones using a structural element (kernel).",
        "The cv2.calcHist() function is used, which allows for counting the frequency of pixel values within specific ranges (bins).",
        "It is used to perform affine transformations on an image, such as translation, rotation, or scaling, based on a 2x3 transformation matrix.",
        "The cv2.meanShift() function should be used, which iteratively shifts a search window toward the direction of maximum probability density.",
        # Open3D
        "You should use the open3d.io.read_point_cloud('file_path') function, which returns a PointCloud class object.",
        "The voxel_down_sample(voxel_size) method is used, where points located within a single voxel are replaced by their average.",
        "ICP is used for registration (alignment) of two point clouds by minimizing the distance between corresponding points to find the spatial transformation.",
        "You should call the estimate_normals() method, which estimates normals based on the local neighborhood of each point (e.g., using a KDTree).",
        "The remove_statistical_outlier() function is used, which removes points that are further from their neighbors than the allowed average distance.",
        "The open3d.visualization.draw_geometries([pcd]) function is used for this purpose.",
        "RANSAC is an iterative method used by the segment_plane() function, which allows finding the mathematical plane model that best fits a set of points.",
        "The transform(matrix) method should be used, where the matrix contains rotation and translation information in homogeneous coordinates.",
        "The create_from_point_cloud_ball_pivoting() function is used, which requires previously calculated surface normals.",
        "The crop(bounding_box) method should be used, where the bounding box is defined as an AxisAlignedBoundingBox or OrientedBoundingBox object.",
        # PCL
        "The PassThrough filter allows for cutting off points whose coordinates in a chosen dimension (e.g., Z-axis) are outside a specified [min, max] range.",
        "This class is used for downsampling point clouds by creating a 3D grid and replacing all points in a voxel with their centroid.",
        "You should create a pcl::EuclideanClusterExtraction object, define a search tree (e.g., KdTree), and set the cluster tolerance distance.",
        "It is a local feature descriptor representing the surface geometry around a point, used for matching and registering point clouds.",
        "This filter removes points that have fewer than a specified minimum number of neighbors within a given search radius.",
        "It is used for fitting geometric models (planes, spheres, cylinders) to a point cloud using Sample Consensus algorithms like RANSAC.",
        "This can be done using the += operator (e.g., cloud_a += cloud_b), which appends the points from one cloud to the end of the other.",
        "MLS is used for smoothing point clouds and improving normal vector estimation through local surface approximation using polynomials.",
        "The pcl::KdTreeFLANN<pcl::PointXYZ> class should be used, which enables fast K-nearest neighbor (KNN) searching.",
        "It is a keypoint feature descriptor designed for point clouds from range sensors, taking object boundaries into account.",
    ],
    "answer": [
        # OpenCV
        "You can use `cv::Canny` or `cv::gapi::Canny`. \n\n\n", 
        "void cv::findContours #include < opencv2/imgproc.hpp >\n\nFinds contours in a binary image. \n\n\nThe function retrieves contours from the binary image using the algorithm  [264]  . The contours are a useful tool for shape analysis and object detection and recognition. See squares.cpp in the OpenCV sample directory. \n",
        "void cv::adaptiveThreshold(InputArray   src , OutputArray   dst , double   maxValue , int   adaptiveMethod , int   thresholdType , int   blockSize , double   C \xa0) \n\n\nPython: cv.adaptiveThreshold( src, maxValue, adaptiveMethod, thresholdType, blockSize, C[, dst] ) ->  dst \n",
        "I cannot find the answer to this in the retrieved documentation.  \n", 
        "You can convert from BGR to Gray using  `cv::hal::cvtBGRtoGray`. \n\nYou can convert from BGR to HSV using `cv::hal::cvtBGRtoHSV`. \n\n\n", 
        "I cannot find the answer to this in the retrieved documentation. \n", 
        "You can perform a dilation operation on a binary image using the `cv::dilate` function. \n\n\nHere is a C++ code example:\n\n```cpp\nvoid cv::dilate(InputArray src, OutputArray dst, InputArray kernel, Point anchor = Point(-1,-1), int iterations = 1, int borderType = BORDER_CONSTANT, const Scalar& borderValue = morphologyDefaultBorderValue());\n```\n\n\n", 
        "You can calculate an image histogram using the `cv::calcHist` function. \n\n\n```cpp\nvoid cv::calcHist(\n    const Mat* images,\n    int nimages,\n    const int* channels,\n    InputArray mask,\n    OutputArray hist,\n    int dims,\n    const int* histSize,\n    const float** ranges,\n    bool uniform,\n    bool accumulate); \n```\n\n", 
        "The function warpAffine transforms the source image using the specified matrix:\n\n\\[\\texttt{dst} (x,y) =  \\texttt{src} ( \\texttt{M} _{11} x +  \\texttt{M} _{12} y +  \\texttt{M} _{13}, \\texttt{M} _{21} x +  \\texttt{M} _{22} y +  \\texttt{M} _{23})\\]\n\nwhen the flag  WARP_INVERSE_MAP  is set. Otherwise, the transformation is first inverted with  invertAffineTransform  and then put in the formula above instead of M. The function cannot operate in-place. \n\n\n", 
        "The function `cv::meanShift` finds an object on a back projection image. \n\nHere's how to use it:\n\n```c++\nint cv::meanShift(InputArray probImage, Rect &window, TermCriteria criteria)\n```\n\n* **probImage:** Back projection of the object histogram. See `calcBackProject` for details.\n* **window:** Initial search window.\n* **criteria:** Stop criteria for the iterative search algorithm. \n\n\nThe function implements the iterative object search algorithm. It takes the input back projection of an object and the initial position. The mass center in window of the back projection image is computed and the search window center shifts to the mass center. The procedure is repeated until the specified number of iterations criteria.maxCount is done or until the window center shifts by less than criteria.epsilon. \n\n\n\n",
        # Open3D
        "You can use the  `open3d.io.read_point_cloud` function to read PointCloud from file. \n\n```python\nopen3d.io.read_point_cloud(filename='path/to/your/file.pcd', format='auto')\n``` \n\n\n", 
        "open3d.geometry.PointCloud.voxel_down_sample(self : open3d.geometry.PointCloud , voxel_size : float ) → open3d.geometry.PointCloud \n\nFunction to downsample input pointcloud into output pointcloud with a voxel. Normals and colors are averaged if they exist.\n\n\n\n", 
        "I cannot find the answer to this in the retrieved documentation. \n\n\n", 
        "Here are some ways to calculate surface normals for points in a cloud based on the provided documentation:\n\n* **Open3D:** \n    * `open3d.geometry.PointCloud.estimate_normals`: Computes normals using KNN search, Radius search, or Hybrid Search.  You can specify parameters like `max_nn` (number of neighbors) and `radius`.\n    * `open3d.t.geometry.PointCloud.orient_normals_consistent_tangent_plane`: Orients normals consistently based on tangent planes.\n\n* **PCL:**\n    * `pcl::device::extractNormals`: Performs normal computation for points using a tsdf volume. \n    * `pcl::device::kinfuLS::extractNormals`: Similar to above, but uses the kinfuLS method.\n\n* **OpenCV:**\n    * `cv::rgbd::DepthCleaner::operator()`: Computes normals at each point given a set of 3D points in a depth image.\n    * `cv::rgbd::RgbdNormals::operator()`: Similar to above, but focuses on computing normals directly.\n    * `cv::ppf_match_3d::computeNormalsPC3d`: Computes normals using a plane fitting approach.\n\n\n\nLet me know if you have any more questions!", 
        "open3d.geometry.PointCloud.remove_statistical_outlier  Removes points that are further away from their neighbors in average. \n\n\n```python\n# Example usage\npoint_cloud = open3d.geometry.PointCloud()\nnb_neighbors = 20\nstd_ratio = 1.5\nfiltered_point_cloud, removed_indices = point_cloud.remove_statistical_outlier(nb_neighbors, std_ratio)\n```", 
        "I cannot find the answer to this in the retrieved documentation. \n\n\n", 
        "RANSAC stands for Random Sample Consensus. It's an algorithm used to estimate the parameters of a model (in this case, a plane) from a set of data points, assuming that most of the data points are inliers (belong to the model) and a small percentage are outliers. \n\nThe algorithm iteratively selects random samples of data points and fits a model to them. The model is then evaluated based on how well it fits the remaining data points.  If enough data points are well-fit by the model, the parameters of the model are accepted as the best estimate.\n\n\n", 
        "```cpp\nvoid pcl::transformPointCloud(\n    const pcl::PointCloud<PointT>& cloud_in,\n    pcl::PointCloud<PointT>& cloud_out,\n    const Eigen::Matrix<Scalar, 4, 4>& transform,\n    bool copy_all_fields = true)\n```\n\nYou can use the `pcl::transformPointCloud` function. It takes as input:\n\n*  `cloud_in`: The input point cloud.\n*  `cloud_out`: The output point cloud where the transformed points will be stored.\n*  `transform`: A 4x4 transformation matrix representing the rigid transformation to apply.\n*  `copy_all_fields`: A flag indicating whether to copy all fields (other than x, y, z) from the input point cloud to the output point cloud.\n\n\n\n", 
        "```cpp\nopen3d.geometry.TriangleMesh.create_from_point_cloud_ball_pivoting(pcd : open3d.geometry.PointCloud , radii : open3d.utility.DoubleVector) → open3d.geometry.TriangleMesh \n``` \n\n", 
        "open3d.geometry.AxisAlignedBoundingBox.get_point_indices_within_bounding_box ( self : open3d.geometry.AxisAlignedBoundingBox , points : open3d.utility.Vector3dVector ) → list [ int ] \n\n```python\n# Example usage:\naabb = open3d.geometry.AxisAlignedBoundingBox()  \npoints = open3d.utility.Vector3dVector() # Your point cloud data\nindices = aabb.get_point_indices_within_bounding_box(points)\n```\n\n\n\n", 
        #PCL
        "The `pcl::PassThrough` filter is used to remove points from a point cloud that fall outside of a specified range for one or more dimensions. \n\nIt works by iterating through each point in the input point cloud and checking if its coordinates are within the defined range. Points that fall outside the range are removed, and the remaining points are stored in the output point cloud.\n\n\n", 
        "Downsample a Point Cloud using a voxelized grid approach. \n", 
        "```cpp\ntemplate<typename PointT>\nvoid pcl::extractEuclideanClusters (\n  const PointCloud<PointT>& cloud,\n  const typename search::Search<PointT>::Ptr& tree,\n  float tolerance,\n  std::vector<PointIndices>& clusters,\n  unsigned int min_pts_per_cluster = 1,\n  unsigned int max_pts_per_cluster = (std::numeric_limits<int>::max)()\n)\n```", 
        "I cannot find the answer to this in the retrieved documentation. \n\n\n",
        "```python\nself.remove_radius_outlier(nb_points=int, radius=float, print_progress=False) \n``` \n\n\n", 
        "Base method for segmentation of a model in a  PointCloud  given by <setInputCloud (), setIndices ()>. \n", 
        "```cpp\ntemplate<typename PointT>\nbool pcl::concatenate(\n  const pcl::PointCloud<PointT>& cloud1,\n  const pcl::PointCloud<PointT>& cloud2,\n  pcl::PointCloud<PointT>& cloud_out\n) \n```", 
        "Smooths a given point and its neighborghood using Moving Least Squares. \n\n\n", 
        "You can define a KDTree for fast neighbor searching in PCL using the following syntax: \n\n```cpp\ntemplate<typename PointInT, typename PointOutT>\nusing pcl::Feature<PointInT, PointOutT>::KdTree = pcl::search::Search<PointInT>;\n```\n\n\n", 
        "I cannot find the answer to this in the retrieved documentation. \n"

    ],
    "contexts": [
    [
        "GMat  cv::gapi::Canny #include < opencv2/gapi/imgproc.hpp >\n\nFinds edges in an image using the Canny algorithm.\n\nThe function finds edges in the input image and marks them in the output map edges using the Canny algorithm. The smallest value between threshold1 and threshold2 is used for edge linking. The largest value is used to find initial segments of strong edges. See  http://en.wikipedia.org/wiki/Canny_edge_detector",
        "void cv::Canny #include < opencv2/imgproc.hpp >\n\nFinds edges in an image using the Canny algorithm  [50]  .\n\nThe function finds edges in the input image and marks them in the output map edges using the Canny algorithm. The smallest value between threshold1 and threshold2 is used for edge linking. The largest value is used to find initial segments of strong edges. See  https://en.wikipedia.org/wiki/Canny_edge_detector",
        "int hal_ni_canny #include < imgproc/src/hal_replacement.hpp >\n\nCanny edge detector.",
        "Ptr <  CannyEdgeDetector  > cv::cuda::createCannyEdgeDetector #include < opencv2/cudaimgproc.hpp >\n\nCreates implementation for  cuda::CannyEdgeDetector  .",
        "virtual void cv::cuda::CannyEdgeDetector::detect Finds edges in an image using the  [50]  algorithm.",
        "IntelligentScissorsMB  & cv::segmentation::IntelligentScissorsMB::setEdgeFeatureCannyParameters Switch edge feature extractor to use Canny edge detector.",
        "float cv::detail::tracking::tbm::TrackerParams::aff_thr_strong Affinity threshold which is used to determine if tracklet and detection should be combined(strong\ndescriptor is used).",
        "void cv::Canny #include < opencv2/imgproc.hpp >\n\nThis is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.\n\nFinds edges in an image using the Canny algorithm with custom image gradient.",
        "void cv::fastcv::dsp::Canny #include < opencv2/fastcv/edges_dsp.hpp >\n\nCanny edge detector applied to a 8 bit grayscale image.",
        "float cv::detail::tracking::tbm::TrackerParams::aff_thr_fast Affinity threshold which is used to determine if tracklet and detection should be combined (fast descriptor is used).",
        "open3d.visualization.rendering.Open3DScene.set_view_size Sets the view size. This should not be used except for rendering to an image"
    ],
    [
        "void cv::findContours #include < opencv2/imgproc.hpp >\n\nFinds contours in a binary image.\n\nThe function retrieves contours from the binary image using the algorithm  [264]  . The contours are a useful tool for shape analysis and object detection and recognition. See squares.cpp in the OpenCV sample directory.",
        "virtual void cv::hfs::HfsSegment::setMinRegionSizeII : set and get the parameter minRegionSizeII. This parameter is used in the third stage mentioned above. It serves the same purpose as minRegionSizeI",
        "GArray <  GArray <  Point  > > cv::gapi::findContours #include < opencv2/gapi/imgproc.hpp >\n\nFinds contours in a binary image.\n\nThe function retrieves contours from the binary image using the algorithm  [264]  . The contours are a useful tool for shape analysis and object detection and recognition. See squares.cpp in the OpenCV sample directory.",
        "virtual void cv::bioinspired::TransientAreasSegmentationModule::getSegmentationPicture access function return the last segmentation result: a boolean picture which is resampled between 0 and 255 for a display purpose",
        "std::tuple<  GArray <  GArray <  Point  > >,  GArray <  Vec4i  > > cv::gapi::findContoursH #include < opencv2/gapi/imgproc.hpp >\n\nFinds contours and their hierarchy in a binary image.\n\nThe function retrieves contours from the binary image using the algorithm  [264]  and calculates their hierarchy. The contours are a useful tool for shape analysis and object detection and recognition. See squares.cpp in the OpenCV sample directory.",
        "anonymous enum #include < opencv2/stereo.hpp >\n\nspeckle removal algorithms. These algorithms have the purpose of removing small regions",
        "std::tuple<  GArray <  GArray <  Point  > >,  GArray <  Vec4i  > > cv::gapi::findContoursH #include < opencv2/gapi/imgproc.hpp >\n\nThis is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.",
        "virtual void cv::hfs::HfsSegment::setSegEgbThresholdII : set and get the parameter segEgbThresholdII. This parameter is used in the third stage mentioned above. It serves the same purpose as segEgbThresholdI. The segmentation result tends to have more regions remained if this value is large and vice versa.",
        "void cv::findContours #include < opencv2/imgproc.hpp >\n\nThis is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.",
        "int cv::optflow::RLOFOpticalFlowParameter::largeWinSize Maximal window size of the support region. If supportRegionType is SR_FIXED this gives the exact support region size. The speed of the RLOF is related to the applied win sizes. The smaller the window size the lower is the runtime, but the more sensitive to noise is the method.",
        "GArray <  GArray <  Point  > > cv::gapi::findContours #include < opencv2/gapi/imgproc.hpp >\n\nThis is an overloaded member function, provided for convenience. It differs from the above function only in what argument(s) it accepts.",
        "open3d.visualization.rendering.Renderer.add_texture Adds a texture. The first parameter is the image, the second parameter is optional and is True if the image is in the sRGB colorspace and False otherwise",
        "using  cv::gapi::imgproc::GFindContoursOutput  = std::tuple< GArray < GArray < Point >>, GArray < Vec4i >> ",
        "open3d.visualization.gui.TabControl.add_tab Adds a tab. The first parameter is the title of the tab, and the second parameter is a widget–normally this is a layout."
    ],
    [
        "double cv::aruco::DetectorParameters::adaptiveThreshConstant constant for adaptive thresholding before finding contours (default 7)",
        "enum  cv::AdaptiveThresholdTypes #include < opencv2/imgproc.hpp >\n\nadaptive threshold algorithm",
        "int hal_ni_adaptiveThreshold #include < imgproc/src/hal_replacement.hpp >\n\nCalculates adaptive threshold.",
        "int cv::aruco::DetectorParameters::adaptiveThreshWinSizeMax maximum window size for adaptive thresholding before finding contours (default 23).",
        "void cv::adaptiveThreshold #include < opencv2/imgproc.hpp >\n\nApplies an adaptive threshold to an array.\n\nThe function transforms a grayscale image to a binary image according to the formulae:\n\nThe function can process the image in-place.",
        "int cv::aruco::DetectorParameters::adaptiveThreshWinSizeMin minimum window size for adaptive thresholding before finding contours (default 3).",
        "#define CV_HAL_ADAPTIVE_THRESH_MEAN_C   0 #include < opencv2/imgproc/hal/interface.h >",
        "std::size_t cv::gapi::onnx::detail::ParamDesc::num_in How many inputs are defined in the operation.",
        "#define CV_HAL_ADAPTIVE_THRESH_GAUSSIAN_C   1 #include < opencv2/imgproc/hal/interface.h >",
        "std::size_t cv::gapi::onnx::detail::ParamDesc::num_out How many outputs are defined in the operation.",
        "int cv::detail::tracking::tbm::TrackerParams::predict How many frames are used to predict bounding box in case",
        "double cv::threshold #include < opencv2/imgproc.hpp >\n\nApplies a fixed-level threshold to each array element.\n\nThe function applies fixed-level thresholding to a multiple-channel array. The function is typically used to get a bi-level (binary) image out of a grayscale image (  compare  could be also used for this purpose) or for removing a noise, that is, filtering out pixels with too small or too large values. There are several types of thresholding supported by the function. They are determined by type parameter.\n\nAlso, the special values  THRESH_OTSU  or  THRESH_TRIANGLE  may be combined with one of the above values. In these cases, the function determines the optimal threshold value using the Otsu's or Triangle algorithm and uses it instead of the specified thresh.",
        "virtual void cv::BackgroundSubtractorKNN::setkNNSamples Sets the k in the kNN. How many nearest neighbours need to match."
    ],
    [
        "void cv::medianBlur #include < opencv2/imgproc.hpp >\n\nBlurs an image using the median filter.\n\nThe function smoothes an image using the median filter with the  \\(\\texttt{ksize} \\times\n\\texttt{ksize}\\) aperture. Each channel of a multi-channel image is processed independently. In-place operation is supported.",
        "open3d.t.geometry.Image.filter_gaussian Return a new image after Gaussian filtering. Possible kernel_size: odd numbers >= 3 are supported.",
        "void pcl::StereoMatching::medianFilter median filter applied on the previously computed disparity map",
        "open3d.geometry.ImageFilterType.Gaussian7 ",
        "int hal_ni_gaussianBlurBinomial #include < imgproc/src/hal_replacement.hpp >\n\nBlurs an image using a symmetric Gaussian filter with square kernel and sigma=0.",
        "open3d.geometry.ImageFilterType.Gaussian3 ",
        "open3d.geometry.ImageFilterType.Gaussian5 ",
        "open3d.t.geometry.Image.pyrdown Return a new downsampled image with pyramid downsampling formed by a chained Gaussian filter (kernel_size = 5, sigma = 1.0) and a resize (ratio = 0.5) operation.",
        "Ptr <  Filter  > cv::cuda::createMedianFilter Performs median filtering for each point of the source image.\n\nOutputs an image that has been filtered using a median-filtering formulation.\n\nIf compiled with CUDA 11 or greater, a wavelet based algorithm is used as described at:  https://cgenglab.github.io/en/publication/sigga22_wmatrix_median/  Yuji Moroto, Nobuyuki Umetani, 2022, \"Constant Time Median Filter Using 2D Wavelet Matrix\", ACM Transactions on Graphics, Volume 41, Issue 6.\n\nFor earlier versions of CUDA, details on the algorithm used can be found in: Green, O., 2017. \"Efficient scalable median filtering using histogram-based operations\", IEEE Transactions on Image Processing, 27(5), pp.2217-2228.",
        "void cv::absdiff #include < opencv2/core.hpp >\n\nCalculates the per-element absolute difference between two arrays or between an array and a scalar.\n\nThe function  cv::absdiff  calculates: Absolute difference between two arrays when they have the same size and type:\n\n\\[\\texttt{dst}(I) =  \\texttt{saturate} (| \\texttt{src1}(I) -  \\texttt{src2}(I)|)\\]\n\nAbsolute difference between an array and a scalar when the second array is constructed from Scalar or has as many elements as the number of channels in  src1 :\n\n\\[\\texttt{dst}(I) =  \\texttt{saturate} (| \\texttt{src1}(I) -  \\texttt{src2} |)\\]\n\nAbsolute difference between a scalar and an array when the first array is constructed from Scalar or has as many elements as the number of channels in  src2 :\n\n\\[\\texttt{dst}(I) =  \\texttt{saturate} (| \\texttt{src1} -  \\texttt{src2}(I) |)\\]\n\nwhere I is a multi-dimensional index of array elements. In case of multi-channel arrays, each channel is processed independently.",
        "open3d.t.pipelines.odometry.OdometryLossParams.depth_outlier_trunc Depth difference threshold used to filter projective associations.",
        "Ptr <  Filter  > cv::cuda::createGaussianFilter #include < opencv2/cudafilters.hpp >\n\nCreates a Gaussian filter.",
        "GMat  cv::gapi::medianBlur #include < opencv2/gapi/imgproc.hpp >\n\nBlurs an image using the median filter.\n\nThe function smoothes an image using the median filter with the  \\(\\texttt{ksize} \\times\n\\texttt{ksize}\\) aperture. Each channel of a multi-channel image is processed independently. Output image must have the same type, size, and number of channels as the input image."
    ],
    [
        "void cv::hal::cvtBGRtoHSV #include < opencv2/imgproc/hal/hal.hpp >",
        "virtual bool cv::cudacodec::NVSurfaceToColorConverter::convert Performs the conversion from the raw YUV Surface output from  VideoReader  to the requested color format. Use this function when you want to convert the raw YUV Surface output from  VideoReader  to more than one color format or you want both the raw Surface output in addition to a color frame.",
        "GMat  cv::gapi::BGR2Gray #include < opencv2/gapi/imgproc.hpp >\n\nConverts an image from BGR color space to gray-scaled.\n\nThe conventional ranges for B, G, and R channel values are 0 to 255. Resulting gray color value computed as\n\n\\[\\texttt{dst} (I)= \\texttt{0.114} * \\texttt{src}(I).B + \\texttt{0.587} * \\texttt{src}(I).G  + \\texttt{0.299} * \\texttt{src}(I).R \\]",
        "GMat  cv::gapi::BGR2RGB #include < opencv2/gapi/imgproc.hpp >\n\nConverts an image from BGR color space to RGB color space.\n\nThe function converts an input image from BGR color space to RGB. The conventional ranges for B, G, and R channel values are 0 to 255.\n\nOutput image is 8-bit unsigned 3-channel image  CV_8UC3 .",
        "void cv::hal::cvtBGRtoGray #include < opencv2/imgproc/hal/hal.hpp >",
        "GMat  cv::gapi::BGR2YUV #include < opencv2/gapi/imgproc.hpp >\n\nConverts an image from BGR color space to YUV color space.\n\nThe function converts an input image from BGR color space to YUV. The conventional ranges for B, G, and R channel values are 0 to 255.\n\nOutput image must be 8-bit unsigned 3-channel image  CV_8UC3 .",
        "int hal_ni_cvtBGRtoHSV #include < imgproc/src/hal_replacement.hpp >\n\nhal_cvtBGRtoHSV",
        "GMat  cv::gapi::BGR2LUV #include < opencv2/gapi/imgproc.hpp >\n\nConverts an image from BGR color space to LUV color space.\n\nThe function converts an input image from BGR color space to LUV. The conventional ranges for B, G, and R channel values are 0 to 255.\n\nOutput image must be 8-bit unsigned 3-channel image  CV_8UC3 .",
        "void cv::hal::cvtHSVtoBGR #include < opencv2/imgproc/hal/hal.hpp >",
        "GMat  cv::gapi::I4202BGR #include < opencv2/gapi/imgproc.hpp >\n\nConverts an image from I420 color space to BGR color space.\n\nThe function converts an input image from I420 color space to BGR. The conventional ranges for B, G, and R channel values are 0 to 255.\n\nOutput image must be 8-bit unsigned 3-channel image.  CV_8UC3 . Width of BGR output image must be the same as width of input image. Height of BGR output image must be equal 2/3 from height of input image.",
        "void cv::hal::cvtGraytoBGR #include < opencv2/imgproc/hal/hal.hpp >",
        "GMat  cv::gapi::YUV2BGR #include < opencv2/gapi/imgproc.hpp >\n\nConverts an image from YUV color space to BGR color space.\n\nThe function converts an input image from YUV color space to BGR. The conventional ranges for B, G, and R channel values are 0 to 255.\n\nOutput image must be 8-bit unsigned 3-channel image  CV_8UC3 .",
        "int hal_ni_cvtHSVtoBGR #include < imgproc/src/hal_replacement.hpp >\n\nhal_cvtHSVtoBGR",
        "GMat  cv::gapi::RGB2Lab #include < opencv2/gapi/imgproc.hpp >\n\nConverts an image from RGB color space to Lab color space.\n\nThe function converts an input image from BGR color space to Lab. The conventional ranges for R, G, and B channel values are 0 to 255.\n\nOutput image must be 8-bit unsigned 3-channel image  CV_8UC1 ."
    ],
    [
        "typedef  SIFT   cv::SiftFeatureDetector #include < opencv2/features2d.hpp >",
        "static  Ptr <  SIFT  > cv::SIFT::create Create  SIFT  with specified descriptorType.",
        "typedef  SIFT   cv::SiftDescriptorExtractor #include < opencv2/features2d.hpp >",
        "virtual void cv::ShapeContextDistanceExtractor::setTransformAlgorithm Set the algorithm used for aligning the shapes.",
        "static  Ptr <  SIFT  > cv::SIFT::create Parameters\n\nnfeatures The number of best features to retain. The features are ranked by their scores (measured in  SIFT  algorithm as the local contrast)\nnOctaveLayers The number of layers in each octave. 3 is the value used in D. Lowe paper. The number of octaves is computed automatically from the image resolution.\ncontrastThreshold The contrast threshold used to filter out weak features in semi-uniform (low-contrast) regions. The larger the threshold, the less features are produced by the detector.\n\nNote The contrast threshold will be divided by nOctaveLayers when the filtering is applied. When nOctaveLayers is set to default and if you want to use the value used in D. Lowe paper, 0.03, set this argument to 0.09.\nParameters\n\nedgeThreshold The threshold used to filter out edge-like features. Note that the its meaning is different from the contrastThreshold, i.e. the larger the edgeThreshold, the less features are filtered out (more features are retained).\nsigma The sigma of the Gaussian applied to the input image at the octave #0. If your image is captured with a weak camera with soft lenses, you might want to reduce the number.\nenable_precise_upscale Whether to enable precise upscaling in the scale pyramid, which maps index \\(\\texttt{x}\\) to \\(\\texttt{2x}\\). This prevents localization bias. The option is disabled by default.",
        "virtual int cv::SIFT::getNFeatures ",
        "virtual void cv::ShapeContextDistanceExtractor::setCostExtractor Set the algorithm used for building the shape context descriptor cost matrix.",
        "virtual void cv::SIFT::setNFeatures ",
        "enum  cv::TrackerKCF::MODE Feature type to be used in the tracking grayscale, colornames, compressed color-names The modes available now:",
        "void cv::detail::computeImageFeatures #include < opencv2/stitching/detail/matchers.hpp >",
        "static void cv::quality::QualityBRISQUE::computeFeatures static method for computing image features used by the BRISQUE algorithm"
    ],
    [
        "int pcl::gpu::people::ProbabilityProcessor::GaussianBlur This will do a GaussianBlur over the LabelProbability.",
        "void  pcl::Morphology <  PointT  >::closingBinary This function performs dilation followed by erosion.\n\nIt is useful for filling up (holes/cracks/small discontinuities) in a binary segmented region\n\nDefinition at line  154  of file  morphology.hpp .",
        "open3d.t.geometry.Image.dilate Return a new image after performing morphological dilation. Supported datatypes are UInt8, UInt16 and Float32 with {1, 3, 4} channels. An 8-connected neighborhood is used to create the dilation mask.",
        "void cv::ximgproc::rl::morphologyEx #include < opencv2/ximgproc/run_length_morphology.hpp >\n\nApplies a morphological operation to a run-length encoded binary image.",
        "void cv::ximgproc::rl::dilate #include < opencv2/ximgproc/run_length_morphology.hpp >\n\nDilates an run-length encoded binary image by using a specific structuring element.",
        "enum  cv::ximgproc::HoughOp Specifies binary operations.\n\nThe enum specifies binary operations, that is such ones which involve two operands. Formally, a binary operation \\( f \\) on a set \\( S \\) is a binary relation that maps elements of the Cartesian product \\( S \\times S \\) to \\( S \\):\n\n\\[ f: S \\times S \\to S \\]",
        "void cv::dilate #include < opencv2/imgproc.hpp >\n\nDilates an image by using a specific structuring element.\n\nThe function dilates the source image using the specified structuring element that determines the shape of a pixel neighborhood over which the maximum is taken:\n\n\\[\\texttt{dst} (x,y) =  \\max _{(x',y'):  \\, \\texttt{element} (x',y') \\ne0 } \\texttt{src} (x+x',y+y')\\]\n\nThe function supports the in-place mode. Dilation can be applied several ( iterations ) times. In case of multi-channel images, each channel is processed independently.",
        "GMat  cv::gapi::dilate #include < opencv2/gapi/imgproc.hpp >\n\nDilates an image by using a specific structuring element.\n\nThe function dilates the source image using the specified structuring element that determines the shape of a pixel neighborhood over which the maximum is taken:\n\n\\[\\texttt{dst} (x,y) =  \\max _{(x',y'):  \\, \\texttt{element} (x',y') \\ne0 } \\texttt{src} (x+x',y+y')\\]\n\nDilation can be applied several (iterations) times. In case of multi-channel images, each channel is processed independently. Supported input matrix data types are  CV_8UC1 ,  CV_8UC3 ,  CV_16UC1 ,  CV_16SC1 , and  CV_32FC1 . Output image must have the same type, size, and number of channels as the input image.",
        "GMat  cv::gapi::dilate3x3 #include < opencv2/gapi/imgproc.hpp >\n\nDilates an image by using 3 by 3 rectangular structuring element.\n\nThe function dilates the source image using the specified structuring element that determines the shape of a pixel neighborhood over which the maximum is taken:\n\n\\[\\texttt{dst} (x,y) =  \\max _{(x',y'):  \\, \\texttt{element} (x',y') \\ne0 } \\texttt{src} (x+x',y+y')\\]\n\nDilation can be applied several (iterations) times. In case of multi-channel images, each channel is processed independently. Supported input matrix data types are  CV_8UC1 ,  CV_8UC3 ,  CV_16UC1 ,  CV_16SC1 , and  CV_32FC1 . Output image must have the same type, size, and number of channels as the input image.",
        "void cv::cuda::bitwise_xor_with_scalar #include < opencv2/cudaarithm.hpp >\n\nPerforms a per-element bitwise exclusive or operation of a matrix and a scalar.",
        "void  pcl::Morphology <  PointT  >::erosionBinary Binary dilation is similar to a logical disjunction of sets.\n\nAt each pixel having value 1, if for all pixels in the structuring element having value 1, the corresponding pixels in the input image are also 1, the center pixel is set to 1. Otherwise, it is set to 0.\n\nDefinition at line  47  of file  morphology.hpp .\n\nReferences  pcl::PointCloud< PointT >::height ,  pcl::PointCloud< PointT >::resize() , and  pcl::PointCloud< PointT >::width .",
        "void cv::ximgproc::thinning #include < opencv2/ximgproc.hpp >\n\nApplies a binary blob thinning operation, to achieve a skeletization of the input image.\n\nThe function transforms a binary blob image into a skeletized form using the technique of Zhang-Suen."
    ],
    [
        "__host__ void cv::cudev::gridHistogram #include < opencv2/cudev/grid/histogram.hpp >",
        "void cv::cuda::calcHist #include < opencv2/cudaimgproc.hpp >\n\nCalculates histogram for one channel 8-bit image confined in given mask.",
        "void cv::calcHist #include < opencv2/imgproc.hpp >\n\nCalculates a histogram of a set of arrays.\n\nThe function  cv::calcHist  calculates the histogram of one or more arrays. The elements of a tuple used to increment a histogram bin are taken from the corresponding input arrays at the same location. The sample below shows how to compute a 2D Hue-Saturation histogram for a color image. :",
        "enum  cv::ximgproc::HoughDeskewOption Specifies to do or not to do skewing of Hough transform image.\n\nThe enum specifies to do or not to do skewing of Hough transform image so it would be no cycling in Hough transform image through borders of image.",
        "void cv::fastcv::calcHist #include < opencv2/fastcv/histogram.hpp >\n\nCalculates histogram of input image. This function implements specific use case of 256-bin histogram calculation for 8u single channel images in an optimized way.",
        "void pcl::visualization::ImageViewer::addRGBImage Add an  RGB  2D image layer, but do not render it (use spin/spinOnce to update).",
        "void cv::cuda::calcHist #include < opencv2/cudaimgproc.hpp >\n\nCalculates histogram for one channel 8-bit image.",
        "void pcl::visualization::ImageViewer::addAngleImage Add an angle 2D image layer, but do not render it (use spin/spinOnce to update).",
        "std::size_t cv::gapi::onnx::detail::ParamDesc::num_out How many outputs are defined in the operation.",
        "std::size_t cv::gapi::onnx::detail::ParamDesc::num_in How many inputs are defined in the operation.",
        "int hal_ni_calcHist #include < imgproc/src/hal_replacement.hpp >\n\nCalculates a histogram of a set of arrays.",
        "open3d.visualization.gui.Application.render_to_image Renders a scene to an image and returns the image. If you are rendering without a visible window you should use open3d.visualization.rendering.RenderToImage instead"
    ],
    [
        "void cv::cuda::warpAffine #include < opencv2/cudawarping.hpp >\n\nApplies an affine transformation to an image.",
        "open3d.geometry.OctreeInternalNode.get_update_function Get lambda function for updating OctreeInternalNode. This update function does nothing.",
        "void cv::warpAffine #include < opencv2/imgproc.hpp >\n\nApplies an affine transformation to an image.\n\nThe function warpAffine transforms the source image using the specified matrix:\n\n\\[\\texttt{dst} (x,y) =  \\texttt{src} ( \\texttt{M} _{11} x +  \\texttt{M} _{12} y +  \\texttt{M} _{13}, \\texttt{M} _{21} x +  \\texttt{M} _{22} y +  \\texttt{M} _{23})\\]\n\nwhen the flag  WARP_INVERSE_MAP  is set. Otherwise, the transformation is first inverted with  invertAffineTransform  and then put in the formula above instead of M. The function cannot operate in-place.",
        "void pcl::gpu::people::RDFBodyPartsDetector::process This function does the complete RDF evaluation in a discrete manner and builds the blob matrix.",
        "void cv::hal::warpAffine #include < opencv2/imgproc/hal/hal.hpp >",
        "void pcl::gpu::people::RDFBodyPartsDetector::processSmooth This smooths the labels and does the connected components.",
        "void cv::fastcv::warpAffine #include < opencv2/fastcv/warp.hpp >\n\nPerforms an affine transformation on an input image using a provided transformation matrix.\n\nThis function performs two types of operations based on the transformation matrix:",
        "void cv::fastFree #include < opencv2/core/cvstd.hpp >\n\nDeallocates a memory buffer.\n\nThe function deallocates the buffer allocated with fastMalloc . If NULL pointer is passed, the function does nothing. C version of the function clears the pointer  pptr  to avoid problems with double memory deallocation.",
        "GMat  cv::gapi::warpAffine #include < opencv2/gapi/core.hpp >\n\nApplies an affine transformation to an image.\n\nThe function warpAffine transforms the source image using the specified matrix:\n\n\\[\\texttt{dst} (x,y) =  \\texttt{src} ( \\texttt{M} _{11} x +  \\texttt{M} _{12} y +  \\texttt{M} _{13}, \\texttt{M} _{21} x +  \\texttt{M} _{22} y +  \\texttt{M} _{23})\\]\n\nwhen the flag  WARP_INVERSE_MAP  is set. Otherwise, the transformation is first inverted with  invertAffineTransform  and then put in the formula above instead of M. The function cannot operate in-place.",
        "int cv::cuda::countNonZero #include < opencv2/cudaarithm.hpp >\n\nCounts non-zero matrix elements.\n\nThe function does not work with CV_64F images on GPUs with the compute capability < 1.3.",
        "Mat  cv::getAffineTransform #include < opencv2/imgproc.hpp >",
        "void  pcl::Edge < PointInT, PointOutT >::computeDerivativeYForward Computes the image derivatives in Y direction using the kernel  kernel::derivativeYForwardKernel .\n\nThis function does NOT include a smoothing step. The image should be smoothed before using this function to reduce noise.",
        "static __host__  AffineMapPtrSz  cv::cudev::affineMap #include < opencv2/cudev/ptr2d/warping.hpp >",
        "void  pcl::Edge < PointInT, PointOutT >::computeDerivativeYCentral Computes the image derivatives in Y direction using the kernel  kernel::derivativeYCentralKernel .\n\nThis function does NOT include a smoothing step. The image should be smoothed before using this function to reduce noise."
    ],
    [
        "int cv::meanShift #include < opencv2/video/tracking.hpp >\n\nFinds an object on a back projection image.",
        "pcl::recognition::ModelLibrary::ModelLibrary This class is used by ' ObjRecRANSAC ' to maintain the object models to be recognized.\n\nNormally, you do not need to use this class directly.",
        "RotatedRect  cv::CamShift #include < opencv2/video/tracking.hpp >\n\nFinds an object center, size, and orientation.\n\nSee the OpenCV sample camshiftdemo.c that tracks colored objects.",
        "bool cv::legacy::MultiTracker::update Update the current tracking status.",
        "int cv::fastcv::meanShift #include < opencv2/fastcv/shift.hpp >\n\nApplies the meanshift procedure and obtains the final converged position. This function applies the meanshift procedure to an original image (usually a probability image) and obtains the final converged position. The converged position search will stop either it has reached the required accuracy or the maximum number of iterations.  Moments  used in the algorithm are calculated in floating point. This function isn't bit-exact with  cv::meanShift  but provides improved latency on Snapdragon processors.",
        "std::size_t cv::gapi::onnx::detail::ParamDesc::num_in How many inputs are defined in the operation.",
        "void cv::cuda::meanShiftProc #include < opencv2/cudaimgproc.hpp >\n\nPerforms a mean-shift procedure and stores information about processed points (their colors and positions) in two images.",
        "std::size_t cv::gapi::onnx::detail::ParamDesc::num_out How many outputs are defined in the operation.",
        "std::tuple<  cv::GArray <  cv::Rect  >,  cv::GArray < int >,  cv::GArray < uint64_t >,  cv::GArray < int > > cv::gapi::ot::track Tracks objects with video frames. If a detected object is overlapped enough with one of tracked object, the tracked object's informationis updated with the input detected object. On the other hand, if a detected object is overlapped with none of tracked objects, the detected object is newly added and ObjectTracker starts to track the object. In zero term tracking type, ObjectTracker clears tracked objects in case that empty list of detected objects is passed in.",
        "void pcl::vtkXRenderWindowInteractor::ProcessEvents Run the event loop and return.\n\nThis is provided so that you can implement your own event loop but yet use the vtk event handling as well.",
        "void cv::DetectionBasedTracker::updateTrackedObjects ",
        "virtual float cv::TrackerDaSiamRPN::getTrackingScore Return tracking score.",
        "double cv::legacy::TrackerMedianFlow::Params::maxMedianLengthOfDisplacementDifference criterion for loosing the tracked object",
        "virtual float cv::TrackerVit::getTrackingScore Return tracking score."
    ],
    [
        "open3d.io.read_point_cloud Function to read PointCloud from file",
        "open3d.ml.tf.dataloaders.TFDataloader This class allows you to load datasets for a TensorFlow framework.",
        "open3d.t.io.read_point_cloud Function to read PointCloud with tensor attributes from file.",
        "void pcl::GeneralizedIterativeClosestPoint6D::setInputSource Provide a pointer to the input source (e.g., the point cloud that we want to align to the target)",
        "open3d.io.write_point_cloud Function to write PointCloud to file",
        "void pcl::GeneralizedIterativeClosestPoint6D::setInputTarget Provide a pointer to the input target (e.g., the point cloud that we want to align the input source to)",
        "open3d.geometry.PointCloud.PointCloud ",
        "open3d.geometry.PointCloud.segment_plane Segments a plane in the point cloud using the RANSAC algorithm.",
        "open3d.io.read_point_cloud_from_bytes Function to read PointCloud from memory",
        "open3d.data.DemoCropPointCloud Data class for DemoCropPointCloud contains a point cloud, and cropped.json (a saved selected polygon volume file). This dataset is used in Open3D for point cloud crop demo.",
        "open3d.data.PLYPointCloud.__init__ ",
        "open3d.geometry.PointCloud.compute_nearest_neighbor_distance Function to compute the distance from a point to its nearest neighbor in the point cloud",
        "open3d.data.PCDPointCloud.__init__ ",
        "open3d.geometry.PointCloud.detect_planar_patches Detects planar patches in the point cloud using a robust statistics-based approach."
    ],
    [
        "open3d.t.pipelines.slac.slac_optimizer_params.voxel_size Voxel size to downsample input point cloud.",
        "open3d.t.geometry.PointCloud.voxel_down_sample Downsamples a point cloud with a specified voxel size.",
        "open3d.geometry.PointCloud.voxel_down_sample Function to downsample input pointcloud into output pointcloud with a voxel. Normals and colors are averaged if they exist.",
        "void pcl::surface::SimplificationRemoveUnusedVertices::simplify Perform simplification (remove unused vertices).",
        "open3d.geometry.PointCloud.voxel_down_sample_and_trace Function to downsample using PointCloud::VoxelDownSample. Also records point cloud index before downsampling",
        "open3d.geometry.PointCloud.segment_plane Segments a plane in the point cloud using the RANSAC algorithm.",
        "open3d.geometry.PointCloud.detect_planar_patches Detects planar patches in the point cloud using a robust statistics-based approach.",
        "open3d.geometry.PointCloud.uniform_down_sample Function to downsample input pointcloud into output pointcloud uniformly. The sample is performed in the order of the points with the 0-th point always chosen, not at random.",
        "open3d.geometry.PointCloud.has_normals Returns True if the point cloud contains point normals.",
        "open3d.t.geometry.PointCloud.uniform_down_sample Downsamples a point cloud by selecting every kth index point and its attributes.",
        "open3d.geometry.PointCloud.has_colors Returns True if the point cloud contains point colors.",
        "open3d.geometry.PointCloud.VoxelGrid ",
        "open3d.t.geometry.TriangleMesh.compute_convex_hull Compute the convex hull of a point cloud using qhull. This runs on the CPU."
    ],
    [
        "open3d.pipelines.registration.registration_icp Function for ICP registration",
        "virtual void cv::hfs::HfsSegment::setMinRegionSizeII : set and get the parameter minRegionSizeII. This parameter is used in the third stage mentioned above. It serves the same purpose as minRegionSizeI",
        "open3d.t.pipelines.registration.icp Function for ICP registration",
        "virtual void cv::LineSegmentDetector::detect Finds lines in the input image.\n\nThis is the output of the default parameters of the algorithm on the above shown image.",
        "open3d.t.pipelines.registration.multi_scale_icp Function for Multi-Scale ICP registration",
        "virtual void cv::ximgproc::FastLineDetector::detect Finds lines in the input image. This is the output of the default parameters of the algorithm on the above shown image.",
        "open3d.pipelines.registration.registration_generalized_icp Function for Generalized ICP registration",
        "open3d.t.pipelines.registration.ICPConvergenceCriteria Convergence criteria of ICP. ICP algorithm stops if the relative change of fitness and rmse hit relative_fitness and relative_rmse individually, or the iteration number exceeds max_iteration .",
        "open3d.pipelines.registration.registration_colored_icp Function for Colored ICP registration",
        "float cv::large_kinfu::Params::icpAngleThresh angle threshold for ICP in radians",
        "open3d.data.DemoICPPointClouds.__init__ ",
        "float cv::kinfu::Params::icpAngleThresh angle threshold for ICP in radians",
        "open3d.data.DemoColoredICPPointClouds.__init__ ",
        "float cv::kinfu::Params::icpDistThresh distance theshold for ICP in meters"
    ],
    [
        "void pcl::device::extractNormals Performs normals computation for given points using tsdf volume.",
        "open3d.geometry.PointCloud.estimate_normals Function to compute the normals of a point cloud. Normals are oriented with respect to the input point cloud if normals exist",
        "void cv::rgbd::DepthCleaner::operator() Given a set of 3d points in a depth image, compute the normals at each point.",
        "open3d.t.geometry.PointCloud.estimate_normals Function to estimate point normals. If the point cloud normals exist, the estimated normals are oriented with respect to the same. It uses KNN search (Not recommended to use on GPU) if only max_nn parameter is provided, Radius search (Not recommended to use on GPU) if only radius is provided and Hybrid Search (Recommended) if radius parameter is also provided.",
        "void cv::rgbd::RgbdNormals::operator() Given a set of 3d points in a depth image, compute the normals at each point.",
        "open3d.geometry.PointCloud.orient_normals_consistent_tangent_plane Function to orient the normals with respect to consistent tangent planes",
        "void pcl::device::kinfuLS::extractNormals Performs normals computation for given points using tsdf volume.",
        "open3d.t.geometry.PointCloud.orient_normals_consistent_tangent_plane Function to consistently orient the normals of a point cloud based on tangent planes.\n\nThe algorithm is described in Hoppe et al., “Surface Reconstruction from Unorganized Points”, 1992.\nAdditional information about the choice of lambda_penalty and cos_alpha_tol for complex\npoint clouds can be found in Piazza, Valentini, Varetti, “Mesh Reconstruction from Point Cloud”, 2023\n( https://eugeniovaretti.github.io/meshreco/Piazza_Valentini_Varetti_MeshReconstructionFromPointCloud_2023.pdf ).",
        "int pcl::Narf::getNoOfBeamPoints How many points on each beam of the gradient star are used to calculate the descriptor?\n\nDefinition at line  151  of file  narf.h .\n\nReferences  pcl_lrint .",
        "int cv::ppf_match_3d::computeNormalsPC3d #include < opencv2/surface_matching/ppf_helpers.hpp >\n\nCompute the normals of an arbitrary point cloud computeNormalsPC3d uses a plane fitting approach to smoothly compute local normals. Normals are obtained through the eigenvector of the covariance matrix, corresponding to the smallest eigen value. If PCNormals is provided to be an Nx6 matrix, then no new allocation is made, instead the existing memory is overwritten.",
        "open3d.t.geometry.PointCloud.rotate Rotate points and normals (if exist).",
        "open3d.geometry.PointCloud.orient_normals_towards_camera_location Function to orient the normals of a point cloud",
        "open3d.t.geometry.TriangleMesh.rotate Rotate points and normals (if exist)."
    ],
    [
        "open3d.geometry.PointCloud.remove_statistical_outlier Removes points that are further away from their neighbors in average.",
        "Mat  cv::ppf_match_3d::addNoisePC #include < opencv2/surface_matching/ppf_helpers.hpp >\n\nAdds a uniform noise in the given scale to the input point cloud",
        "open3d.t.geometry.PointCloud.remove_statistical_outliers Remove points that are further away from their p nb_neighbor\nneighbors in average. This function is not recommended to use on GPU.",
        "open3d.geometry.PointCloud.segment_plane Segments a plane in the point cloud using the RANSAC algorithm.",
        "open3d.geometry.PointCloud.remove_radius_outlier Removes points that have neighbors less than nb_points in a sphere of a given radius",
        "open3d.geometry.PointCloud.compute_nearest_neighbor_distance Function to compute the distance from a point to its nearest neighbor in the point cloud",
        "open3d.t.geometry.PointCloud.remove_radius_outliers Remove points that have less than nb_points neighbors in a\nsphere of a given search radius.",
        "open3d.geometry.PointCloud.detect_planar_patches Detects planar patches in the point cloud using a robust statistics-based approach.",
        "open3d.t.geometry.PointCloud.remove_duplicated_points Remove duplicated points and there associated attributes.",
        "void pcl::outofcore::queryBBIntersects_noload Non-class method which performs a bounding box query without loading any of the point cloud data from disk.",
        "open3d.t.geometry.PointCloud.hidden_point_removal Removes hidden points from a point cloud and returns a mesh of\nthe remaining points. Based on Katz et al. ‘Direct Visibility of Point Sets’,\n2007. Additional information about the choice of radius for noisy point clouds\ncan be found in Mehra et. al. ‘Visibility of Noisy Point Cloud Data’, 2010.\nThis is a wrapper for a CPU implementation and a copy of the point cloud data\nand resulting visible triangle mesh and indiecs will be made.",
        "void queryBBIntersects_noload Non-class method which performs a bounding box query without loading any of the point cloud data from disk.",
        "open3d.ml.tf.datasets.utils.DataProcessing.remove_outside_points Remove points which are outside of image.",
        "pcl::PointCloud <  pcl::PointXYZ  >::Ptr pcl::ONIGrabber::convertToXYZPointCloud internal method to assemble a point cloud object"
    ],
    [
        "open3d.io.rpc.set_point_cloud Sends a point cloud message to a viewer.",
        "void pcl::visualization::PCLVisualizer::addOrientationMarkerWidgetAxes Adds a widget which shows an interactive axes display for orientation.",
        "open3d.ml.tf.vis.Model.create_point_cloud Create a point cloud based on the data provided.\n\nThe data should include name and points.",
        "static void cvv::debugDMatch #include < opencv2/cvv/dmatch.hpp >\n\nAdd a filled in DMatch <dmatch> to debug GUI.\n\nThe matches can are visualized for interactive inspection in different GUI views (one similar to an interactive :draw_matches:drawMatches<>).",
        "open3d.ml.tf.vis.Visualizer.visualize Visualize a custom point cloud data.\n\nExample\n\nMinimal example for visualizing a single point cloud with an\nattribute:\n\nimport numpy as np import open3d.ml.torch as ml3d # or import open3d.ml.tf as ml3d data = [ { 'name' : 'my_point_cloud' , 'points' : np . random . rand ( 100 , 3 ) . astype ( np . float32 ), 'point_attr1' : np . random . rand ( 100 ) . astype ( np . float32 ), } ] vis = ml3d . vis . Visualizer () vis . visualize ( data )",
        "pcl::PointCloud <  pcl::PointXYZ  >::Ptr pcl::ONIGrabber::convertToXYZPointCloud internal method to assemble a point cloud object",
        "open3d.visualization.VisualizerWithVertexSelection.capture_depth_point_cloud Function to capture and save local point cloud",
        "pcl::PointCloud <  pcl::PointXYZRGBA  >::Ptr pcl::ONIGrabber::convertToXYZRGBAPointCloud internal method to assemble a point cloud object",
        "open3d.ml.torch.vis.Visualizer.visualize Visualize a custom point cloud data.\n\nExample\n\nMinimal example for visualizing a single point cloud with an\nattribute:\n\nimport numpy as np import open3d.ml.torch as ml3d # or import open3d.ml.tf as ml3d data = [ { 'name' : 'my_point_cloud' , 'points' : np . random . rand ( 100 , 3 ) . astype ( np . float32 ), 'point_attr1' : np . random . rand ( 100 ) . astype ( np . float32 ), } ] vis = ml3d . vis . Visualizer () vis . visualize ( data )",
        "pcl::PointCloud <  pcl::PointXYZI  >::Ptr pcl::ONIGrabber::convertToXYZIPointCloud internal method to assemble a point cloud object",
        "open3d.visualization.VisualizerWithEditing.capture_depth_point_cloud Function to capture and save local point cloud",
        "pcl::PointCloud <  pcl::PointXYZRGB  >::Ptr pcl::ONIGrabber::convertToXYZRGBPointCloud internal method to assemble a point cloud object",
        "open3d.visualization.VisualizerWithKeyCallback.capture_depth_point_cloud Function to capture and save local point cloud",
        "open3d.geometry.PointCloud.segment_plane Segments a plane in the point cloud using the RANSAC algorithm."
    ],
    [
        "open3d.t.geometry.PointCloud.segment_plane Segments a plane in the point cloud using the RANSAC algorithm.\nThis is a wrapper for a CPU implementation and a copy of the point cloud data and\nresulting plane model and inlier indiecs will be made.",
        "open3d.geometry.PointCloud.segment_plane Segments a plane in the point cloud using the RANSAC algorithm.",
        "open3d.pipelines.registration.registration_ransac_based_on_correspondence Function for global RANSAC registration based on a set of correspondences",
        "Strides  cv::MediaFrame::View::stride Array of image plane strides, in bytes.",
        "open3d.pipelines.registration.registration_ransac_based_on_feature_matching Function for global RANSAC registration based on feature matching",
        "virtual void cv::ShapeContextDistanceExtractor::setShapeContextWeight Set the weight of the shape context distance in the final value of the shape distance. The shape context distance between two shapes is defined as the symmetric sum of shape context matching costs over best matching points. The final value of the shape distance is a user-defined linear combination of the shape context distance, an image appearance distance, and a bending energy.",
        "open3d.geometry.PointCloud.detect_planar_patches Detects planar patches in the point cloud using a robust statistics-based approach.",
        "virtual void cv::ximgproc::segmentation::SelectiveSearchSegmentation::addGraphSegmentation Add a new graph segmentation in the list of graph segementations to process.",
        "open3d.t.pipelines.odometry.Method.PointToPlane ",
        "void cv::cann::resetDevice #include < opencv2/cann.hpp >\n\nClear all context created in current Ascend device.",
        "open3d.pipelines.registration.RANSACConvergenceCriteria.confidence Desired probability of success. Used for estimating early termination. Use 1.0 to avoid early termination.",
        "virtual int cv::ximgproc::SuperpixelSEEDS::getNumberOfSuperpixels Calculates the superpixel segmentation on a given image stored in  SuperpixelSEEDS  object.\n\nThe function computes the superpixels segmentation of an image with the parameters initialized with the function  createSuperpixelSEEDS() ."
    ],
    [
        "Mat  cv::ppf_match_3d::transformPCPose #include < opencv2/surface_matching/ppf_helpers.hpp >\n\nTransforms the point cloud with a given a homogeneous 4x4 pose matrix (in double precision)",
        "open3d.geometry.PointCloud.transform Apply transformation (4x4 matrix) to the geometry coordinates.",
        "open3d.t.geometry.PointCloud.project_to_depth_image Project a point cloud to a depth image.",
        "open3d.t.geometry.PointCloud.transform Transforms the points and normals (if exist).",
        "open3d.io.rpc.set_point_cloud Sends a point cloud message to a viewer.",
        "void pcl::transformPointCloud #include <pcl/common/impl/transforms.hpp>\n\nApply a rigid transform defined by a 4x4 matrix.\n\nDefinition at line  263  of file  transforms.hpp .\n\nReferences  pcl::PointCloud< PointT >::header ,  pcl::PointCloud< PointT >::height ,  pcl::PointCloud< PointT >::is_dense ,  pcl::PointCloud< PointT >::resize() ,  pcl::detail::Transformer< Scalar >::se3() ,  pcl::PointCloud< PointT >::sensor_orientation_ ,  pcl::PointCloud< PointT >::sensor_origin_ , and  pcl::PointCloud< PointT >::width .",
        "open3d.t.pipelines.registration.TransformationEstimationForColoredICP Class to estimate a transformation between two point clouds using color information",
        "void pcl::transformPointCloud #include <pcl/common/impl/transforms.hpp>\n\nApply a rigid transform defined by a 4x4 matrix.\n\nDefinition at line  221  of file  transforms.hpp .\n\nReferences  pcl::PointCloud< PointT >::assign() ,  pcl::PointCloud< PointT >::begin() ,  pcl::PointCloud< PointT >::data() ,  pcl::PointCloud< PointT >::end() ,  pcl::PointCloud< PointT >::header ,  pcl::PointCloud< PointT >::height ,  pcl::PointCloud< PointT >::is_dense ,  pcl::PointCloud< PointT >::reserve() ,  pcl::PointCloud< PointT >::resize() ,  pcl::detail::Transformer< Scalar >::se3() ,  pcl::PointCloud< PointT >::sensor_orientation_ ,  pcl::PointCloud< PointT >::sensor_origin_ ,  pcl::PointCloud< PointT >::size() , and  pcl::PointCloud< PointT >::width .\n\nReferenced by  ObjectRecognition::alignModelPoints() ,  pcl::people::GroundBasedPeopleDetectionApp< PointT >::applyTransformationPointCloud() ,  pcl::registration::ELCH< PointT >::compute() ,  pcl::GASDEstimation< PointInT, PointOutT >::computeFeature() ,  pcl::OURCVFHEstimation< PointInT, PointNT, PointOutT >::computeRFAndShapeDistribution() ,  pcl::NormalDistributionsTransform< PointSource, PointTarget, Scalar >::computeStepLengthMT() ,  pcl::registration::FPCSInitialAlignment< PointSource, PointTarget, NormalT, Scalar >::computeTransformation() ,  pcl::SampleConsensusInitialAlignment< PointSource, PointTarget, FeatureT >::computeTransformation() ,  pcl::NormalDistributionsTransform2D< PointSource, PointTarget >::computeTransformation() ,  pcl::SampleConsensusPrerejective< PointSource, PointTarget, FeatureT >::computeTransformation() ,  pcl::GeneralizedIterativeClosestPoint< PointSource, PointTarget, Scalar >::computeTransformation() ,  pcl::NormalDistributionsTransform< PointSource, PointTarget, Scalar >::computeTransformation() ,  pcl::registration::LUM< PointT >::getConcatenatedCloud() ,  pcl::kinfuLS::WorldModel< pcl::PointXYZI >::getExistingData() ,  pcl::SampleConsensusPrerejective< PointSource, PointTarget, FeatureT >::getFitness() ,  pcl::Registration< PointSource, PointTarget, Scalar >::getFitnessScore() ,  pcl::gpu::kinfuLS::StandaloneMarchingCubes< PointT >::getMeshesFromTSDFVector() ,  pcl::registration::LUM< PointT >::getTransformedCloud() ,  pcl::TextureMapping< PointInT >::mapMultipleTexturesToMeshUV() ,  pcl::ConcaveHull< PointInT >::performReconstruction() ,  pcl::registration::MetaRegistration< PointT, Scalar >::registerCloud() ,  pcl::ESFEstimation< PointInT, PointOutT >::scale_points_unit_sphere() ,  pcl::OURCVFHEstimation< PointInT, PointNT, PointOutT >::sgurf() ,  pcl::TextureMapping< PointInT >::sortFacesByCamera() ,  pcl::TextureMapping< PointInT >::textureMeshwithMultipleCameras() ,  pcl::transformPointCloud() ,  pcl::registration::FPCSInitialAlignment< PointSource, PointTarget, NormalT, Scalar >::validateMatch() ,  pcl::registration::FPCSInitialAlignment< PointSource, PointTarget, NormalT, Scalar >::validateTransformation() , and  pcl::registration::KFPCSInitialAlignment< PointSource, PointTarget, NormalT, Scalar >::validateTransformation() .",
        "open3d.pipelines.registration.TransformationEstimationForColoredICP Class to estimate a transformation between two point clouds using color information",
        "void pcl::transformPointCloud #include <pcl/common/transforms.h>\n\nApply a rigid transform defined by a 4x4 matrix.\n\nDefinition at line  280  of file  transforms.h .\n\nReferences  pcl::PointIndices::indices .",
        "open3d.t.pipelines.registration.TransformationEstimationForDopplerICP Class to estimate a transformation between two point clouds using color information",
        "open3d.geometry.Geometry3D.transform Apply transformation (4x4 matrix) to the geometry coordinates.",
        "void  pcl::registration::TransformationEstimationPointToPlaneWeighted < PointSource, PointTarget, MatScalar >::estimateRigidTransformation Estimate a rigid rotation transformation between a source and a target point cloud using LM.\n\nDefinition at line  250  of file  transformation_estimation_point_to_plane_weighted.hpp ."
    ],
    [
        "open3d.geometry.TriangleMesh.create_from_point_cloud_ball_pivoting Function that computes a triangle mesh from a oriented PointCloud. This implements the Ball Pivoting algorithm proposed in F. Bernardini et al., “The ball-pivoting algorithm for surface reconstruction”, 1999. The implementation is also based on the algorithms outlined in Digne, “An Analysis and Implementation of a Parallel Ball Pivoting Algorithm”, 2014. The surface reconstruction is done by rolling a ball with a given radius over the point cloud, whenever the ball touches three points a triangle is created.",
        "open3d.geometry.TetraMesh.create_from_point_cloud Function to create a tetrahedral mesh from a point cloud.",
        "open3d.geometry.TriangleMesh.create_from_point_cloud_poisson Function that computes a triangle mesh from a oriented PointCloud pcd. This implements the Screened Poisson Reconstruction proposed in Kazhdan and Hoppe, “Screened Poisson Surface Reconstruction”, 2013. This function uses the original implementation by Kazhdan. See https://github.com/mkazhdan/PoissonRecon",
        "open3d.geometry.PointCloud.TriangleMesh ",
        "open3d.t.geometry.TriangleMesh.create_text Create a triangle mesh from a text string.",
        "open3d.geometry.TriangleMesh.PointCloud ",
        "open3d.geometry.LineSet.create_from_triangle_mesh Factory function to create a LineSet from edges of a triangle mesh.",
        "open3d.pipelines.integration.UniformTSDFVolume.extract_triangle_mesh Function to extract a triangle mesh",
        "open3d.geometry.PointCloud.HalfEdgeTriangleMesh ",
        "open3d.pipelines.integration.ScalableTSDFVolume.extract_triangle_mesh Function to extract a triangle mesh",
        "open3d.geometry.PointCloud.Type.TriangleMesh ",
        "open3d.pipelines.integration.TSDFVolume.extract_triangle_mesh Function to extract a triangle mesh"
    ],
    [
        "int pcl::outofcore::OutofcoreOctreeNodeMetadata::loadMetadataFromDisk Loads the data from a JSON file located at  metadata_filename_ .",
        "int pcl::gpu::people::ProbabilityProcessor::GaussianBlur This will do a GaussianBlur over the LabelProbability.",
        "open3d.t.geometry.AxisAlignedBoundingBox.get_point_indices_within_bounding_box Indices to points that are within the bounding box.",
        "open3d.visualization.SelectionPolygonVolume.bounding_polygon Bounding polygon boundary.",
        "open3d.geometry.AxisAlignedBoundingBox.get_point_indices_within_bounding_box Return indices to points that are within the bounding box.",
        "open3d.t.geometry.OrientedBoundingBox.get_point_indices_within_bounding_box Indices to points that are within the bounding box.",
        "open3d.geometry.OrientedBoundingBox.get_point_indices_within_bounding_box Return indices to points that are within the bounding box.",
        "void pcl::outofcore::OutofcoreOctreeNodeMetadata::serializeMetadataToDisk Writes the data to a JSON file located at  metadata_filename_ .",
        "open3d.geometry.PointCloud.crop Overloaded function.\n\ncrop(self: open3d.geometry.PointCloud, bounding_box: open3d.geometry.AxisAlignedBoundingBox, invert: bool = False) -> open3d.geometry.PointCloud Function to crop input pointcloud into output pointcloud",
        "cv::Vec2f  cv::detail::tracking::tbm::TrackerParams::bbox_heights_range Bounding box heights range.",
        "open3d.t.geometry.AxisAlignedBoundingBox.get_box_points Returns the eight points that define the bounding box. The Return tensor has shape {8, 3} and data type of float32.",
        "open3d.t.geometry.PointCloud.crop Overloaded function.\n\ncrop(self: open3d.t.geometry.PointCloud, aabb: open3d.t.geometry.AxisAlignedBoundingBox, invert: bool = False) -> open3d.t.geometry.PointCloud Function to crop pointcloud into output pointcloud."
    ],
    [
        "using  pcl::PassThrough <  PointT  > ::PointCloud  =  typename  FilterIndices < PointT > ::PointCloud Definition at line  84  of file  passthrough.h .",
        "void cv::fastcv::bilateralFilter #include < opencv2/fastcv/bilateralFilter.hpp >\n\nApplies Bilateral filter to an image considering d-pixel diameter of each pixel's neighborhood. This filter does not work inplace.",
        "void  pcl::PassThrough <  pcl::PCLPointCloud2  >::applyFilter Abstract filter method for point cloud.\n\nImplements  pcl::FilterIndices< pcl::PCLPointCloud2 > .",
        "std::size_t cv::gapi::onnx::detail::ParamDesc::num_out How many outputs are defined in the operation.",
        "pcl::PassThrough <PointInT>  pcl::tracking::ParticleFilterTracker < PointInT, StateT >::pass_x_ Pass through filter to crop the pointclouds within the hypothesis bounding box.\n\nDefinition at line  599  of file  particle_filter.h .\n\nReferenced by  pcl::tracking::ParticleFilterTracker< PointInT, StateT >::ParticleFilterTracker() .",
        "std::size_t cv::gapi::onnx::detail::ParamDesc::num_in How many inputs are defined in the operation.",
        "void  pcl_cuda::PassThrough < CloudT >::applyFilter Filter  a Point Cloud.\n\nImplements  pcl_cuda::Filter< CloudT > .\n\nDefinition at line  105  of file  passthrough.h .",
        "virtual void cv::BackgroundSubtractorKNN::setkNNSamples Sets the k in the kNN. How many nearest neighbours need to match.",
        "using  pcl::PassThrough <  PointT  >::Ptr =  shared_ptr< PassThrough < PointT > > Definition at line  91  of file  passthrough.h .",
        "virtual  Rect  cv::ximgproc::DisparityWLSFilter::getROI Get the ROI used in the last filter call.",
        "pcl::PassThrough <PointInT>  pcl::tracking::ParticleFilterTracker < PointInT, StateT >::pass_z_ Pass through filter to crop the pointclouds within the hypothesis bounding box.\n\nDefinition at line  605  of file  particle_filter.h .\n\nReferenced by  pcl::tracking::ParticleFilterTracker< PointInT, StateT >::ParticleFilterTracker() .",
        "open3d.ml.tf.ops.sparse_conv_transpose_backprop_filter Computes the backrop for the filter of the SparseConvTranspose",
        "pcl::PassThrough <  pcl::PCLPointCloud2  >::PassThrough Constructor.\n\nDefinition at line  214  of file  passthrough.h .",
        "open3d.ml.tf.ops.continuous_conv_backprop_filter Computes the backprop for the filter of the ContinuousConv"
    ],
    [
        "void  pcl::VoxelGrid <  pcl::PCLPointCloud2  >::applyFilter Downsample a Point Cloud using a voxelized grid approach.\n\nImplements  pcl::Filter< pcl::PCLPointCloud2 > .",
        "virtual void cv::hfs::HfsSegment::setMinRegionSizeII : set and get the parameter minRegionSizeII. This parameter is used in the third stage mentioned above. It serves the same purpose as minRegionSizeI",
        "void  pcl::VoxelGrid <  PointT  >::applyFilter Downsample a Point Cloud using a voxelized grid approach.\n\nImplements  pcl::Filter< PointT > .\n\nReimplemented in  pcl::VoxelGridCovariance< PointT > ,  pcl::VoxelGridCovariance< PointTarget > , and  pcl::VoxelGridLabel .\n\nDefinition at line  597  of file  voxel_grid.hpp .\n\nReferences  pcl::internal::cloud_point_index_idx::idx , and  pcl::isXYZFinite() .",
        "virtual void cv::bioinspired::TransientAreasSegmentationModule::getSegmentationPicture access function return the last segmentation result: a boolean picture which is resampled between 0 and 255 for a display purpose",
        "pcl::VoxelGrid < PointT >  pcl::CrfSegmentation <  PointT  >::voxel_grid_ Voxel grid to discretize the scene.\n\nDefinition at line  145  of file  crf_segmentation.h .",
        "anonymous enum #include < opencv2/stereo.hpp >\n\nspeckle removal algorithms. These algorithms have the purpose of removing small regions",
        "using  pcl::VoxelGrid <  PointT  > ::PointCloud  =  typename  Filter < PointT > ::PointCloud Definition at line  229  of file  voxel_grid.h .",
        "Eigen::Vector3i  pcl::VoxelGrid <  pcl::PCLPointCloud2  >::getMaxBoxCoordinates Get the maximum coordinates of the bounding box (after filtering is performed).\n\nDefinition at line  652  of file  voxel_grid.h .",
        "void  pcl::ApproximateVoxelGrid <  PointT  >::applyFilter Downsample a Point Cloud using a voxelized grid approach.\n\nImplements  pcl::Filter< PointT > .\n\nDefinition at line  66  of file  approximate_voxel_grid.hpp .\n\nReferences  pcl::isXYZFinite() .",
        "Eigen::Vector3i  pcl::VoxelGrid <  pcl::PCLPointCloud2  >::getMinBoxCoordinates Get the minimum coordinates of the bounding box (after filtering is performed).\n\nDefinition at line  646  of file  voxel_grid.h .",
        "void  pcl::CrfSegmentation <  PointT  >::createVoxelGrid Create a voxel grid to discretize the scene.\n\nDefinition at line  142  of file  crf_segmentation.hpp .\n\nReferences  pcl::Filter< PointT >::filter() ,  pcl::VoxelGrid< PointT >::setDownsampleAllData() ,  pcl::PCLBase< PointT >::setInputCloud() , and  pcl::VoxelGrid< PointT >::setLeafSize() .",
        "virtual void cv::hfs::HfsSegment::setSegEgbThresholdII : set and get the parameter segEgbThresholdII. This parameter is used in the third stage mentioned above. It serves the same purpose as segEgbThresholdI. The segmentation result tends to have more regions remained if this value is large and vice versa.",
        "pcl::VoxelGrid <  pcl::PCLPointCloud2  >::~ VoxelGrid Destructor.",
        "Eigen::Vector3i  pcl::VoxelGrid <  PointT  >::getMinBoxCoordinates Get the minimum coordinates of the bounding box (after filtering is performed).\n\nDefinition at line  326  of file  voxel_grid.h .\n\nReferences  pcl::VoxelGrid< PointT >::min_b_ ."
    ],
    [
        "void pcl::extractEuclideanClusters #include <pcl/segmentation/extract_clusters.h>\n\nDecompose a region of space into clusters based on the Euclidean distance between points.\n\nDefinition at line  46  of file  extract_clusters.hpp .\n\nReferences  pcl::search::Search< PointT >::getInputCloud() ,  pcl::search::Search< PointT >::getSortedResults() ,  pcl::PointCloud< PointT >::header ,  pcl::PointIndices::header ,  pcl::PointIndices::indices ,  pcl::search::Search< PointT >::radiusSearch() , and  pcl::PointCloud< PointT >::size() .",
        "std::size_t cv::gapi::onnx::detail::ParamDesc::num_in How many inputs are defined in the operation.",
        "void pcl::extractEuclideanClusters #include <pcl/segmentation/extract_clusters.h>\n\nDecompose a region of space into clusters based on the Euclidean distance between points.\n\nDefinition at line  125  of file  extract_clusters.hpp .\n\nReferences  pcl::search::Search< PointT >::getIndices() ,  pcl::search::Search< PointT >::getInputCloud() ,  pcl::search::Search< PointT >::getSortedResults() ,  pcl::PointCloud< PointT >::header ,  pcl::PointIndices::header ,  pcl::PointIndices::indices ,  pcl::search::Search< PointT >::radiusSearch() , and  pcl::PointCloud< PointT >::size() .",
        "std::size_t cv::gapi::onnx::detail::ParamDesc::num_out How many outputs are defined in the operation.",
        "void pcl::extractEuclideanClusters #include <pcl/segmentation/extract_clusters.h>\n\nDecompose a region of space into clusters based on the euclidean distance between points, and the normal angular deviation between points.\n\nEach point added to the cluster is origin to another radius search. Each point within radius range will be compared to the origin in respect to normal angle and euclidean distance. If both are under their respective threshold the point will be added to the cluster. Generally speaking the cluster algorithm will not stop on smooth surfaces but on surfaces with sharp edges.\n\nDefinition at line  103  of file  extract_clusters.h .\n\nReferences  pcl::KdTree< PointT >::getInputCloud() ,  pcl::KdTree< PointT >::getSortedResults() ,  pcl::PointCloud< PointT >::header ,  pcl::PointIndices::header ,  pcl::PointIndices::indices ,  pcl::PointCloud< PointT >::push_back() ,  pcl::KdTree< PointT >::radiusSearch() , and  pcl::PointCloud< PointT >::size() .",
        "size_t cv::detail::tracking::tbm::Track::lost How many frames ago track has been lost.",
        "void pcl::extractLabeledEuclideanClusters #include <pcl/segmentation/extract_labeled_clusters.h>\n\nDecompose a region of space into clusters based on the Euclidean distance between points.\n\nDefinition at line  46  of file  extract_labeled_clusters.hpp .\n\nReferences  pcl::search::Search< PointT >::getInputCloud() ,  pcl::search::Search< PointT >::getSortedResults() ,  pcl::PointCloud< PointT >::header ,  pcl::PointIndices::header ,  pcl::PointIndices::indices ,  pcl::search::Search< PointT >::radiusSearch() , and  pcl::PointCloud< PointT >::size() .",
        "void pcl::vtkXRenderWindowInteractor::ProcessEvents Run the event loop and return.\n\nThis is provided so that you can implement your own event loop but yet use the vtk event handling as well.",
        "void pcl::extractEuclideanClusters #include <pcl/segmentation/extract_clusters.h>\n\nDecompose a region of space into clusters based on the euclidean distance between points, and the normal angular deviation between points.\n\nEach point added to the cluster is origin to another radius search. Each point within radius range will be compared to the origin in respect to normal angle and euclidean distance. If both are under their respective threshold the point will be added to the cluster. Generally speaking the cluster algorithm will not stop on smooth surfaces but on surfaces with sharp edges.\n\nDefinition at line  218  of file  extract_clusters.h .\n\nReferences  pcl::KdTree< PointT >::getIndices() ,  pcl::KdTree< PointT >::getInputCloud() ,  pcl::KdTree< PointT >::getSortedResults() ,  pcl::PointCloud< PointT >::header ,  pcl::PointIndices::header ,  pcl::PointIndices::indices ,  pcl::PointCloud< PointT >::push_back() ,  pcl::KdTree< PointT >::radiusSearch() , and  pcl::PointCloud< PointT >::size() .",
        "void  pcl::JointIterativeClosestPoint < PointSource, PointTarget, Scalar >::addCorrespondenceEstimation Add a manual correspondence estimator If you choose to do this, you must add one for each input source / target pair.\n\nThey do not need to have trees or input clouds set ahead of time.\n\nDefinition at line  194  of file  joint_icp.h .\n\nReferences  pcl::JointIterativeClosestPoint< PointSource, PointTarget, Scalar >::correspondence_estimations_ .",
        "void  pcl::gpu::EuclideanClusterExtraction <  PointT  >::extract extract clusters of a  PointCloud  given by <setInputCloud(), setIndices()>\n\nDefinition at line  189  of file  gpu_extract_clusters.hpp .",
        "virtual  Mat  cv::hfs::HfsSegment::performSegmentGpu do segmentation gpu",
        "pcl::EuclideanClusterExtraction <  PointT  >::EuclideanClusterExtraction Empty constructor.",
        "pcl::recognition::ModelLibrary::ModelLibrary This class is used by ' ObjRecRANSAC ' to maintain the object models to be recognized.\n\nNormally, you do not need to use this class directly."
    ],
    [
        "open3d.pipelines.registration.compute_fpfh_feature Function to compute FPFH feature for a point cloud",
        "void pcl::device::computeFPFH ",
        "void  pcl::FPFHEstimation < PointInT, PointNT, PointOutT >::weightPointSPFHSignature Weight the SPFH (Simple Point  Feature  Histograms) individual histograms to create the final FPFH (Fast Point  Feature   Histogram ) for a given point based on its 3D spatial neighborhood.\n\nDefinition at line  110  of file  fpfh.hpp .",
        "open3d.t.pipelines.registration.compute_fpfh_feature Function to compute FPFH feature for a point cloud.\nIt uses KNN search (Not recommended to use on GPU) if only max_nn parameter\nis provided, Radius search (Not recommended to use on GPU) if only radius\nparameter is provided, and Hybrid search (Recommended) if both are provided.",
        "void  pcl::FPFHEstimation < PointInT, PointNT, PointOutT >::computeSPFHSignatures Estimate the set of all SPFH (Simple Point  Feature  Histograms) signatures for the input cloud.\n\nDefinition at line  182  of file  fpfh.hpp .",
        "void  pcl::VFHEstimation < PointInT, PointNT, PointOutT >::computePointSPFHSignature Estimate the SPFH (Simple Point  Feature  Histograms) signatures of the angular (f1, f2, f3) and distance (f4) features for a given point from its neighborhood.\n\nDefinition at line  92  of file  vfh.hpp .\n\nReferences  pcl::computePairFeatures() ,  pcl::getMaxDistance() ,  M_PI , and  pcl_round() .",
        "void  pcl::FPFHEstimation < PointInT, PointNT, PointOutT >::computePointSPFHSignature Estimate the SPFH (Simple Point  Feature  Histograms) individual signatures of the three angular (f1, f2, f3) features for a given point based on its spatial neighborhood of 3D points with normals.\n\nDefinition at line  64  of file  fpfh.hpp .\n\nReferences  pcl::computePairFeatures() , and  M_PI .",
        "void  pcl::FPFHEstimation < PointInT, PointNT, PointOutT >::computeFeature Estimate the Fast Point  Feature  Histograms (FPFH) descriptors at a set of points given by <setInputCloud (), setIndices ()> using the surface in setSearchSurface () and the spatial locator in setSearchMethod ()\n\nImplements  pcl::Feature< PointInT, PointOutT > .\n\nDefinition at line  238  of file  fpfh.hpp .\n\nReferences  pcl::isFinite() .",
        "Feature  cv::linemod::QuantizedPyramid::Candidate::f ",
        "constexpr pcl::FPFHSignature33::FPFHSignature33 ",
        "pcl::gpu::FPFHEstimation::FPFHEstimation ",
        "Point  cv::PyRotationWarper::warp Projects the image."
    ],
    [
        "open3d.geometry.PointCloud.remove_radius_outlier Removes points that have neighbors less than nb_points in a sphere of a given radius",
        "float cv::videostab::RansacParams::eps max outliers ratio",
        "open3d.t.geometry.PointCloud.remove_radius_outliers Remove points that have less than nb_points neighbors in a\nsphere of a given search radius.",
        "open3d.visualization.Visualizer.remove_geometry Function to remove geometry",
        "open3d.geometry.PointCloud.remove_statistical_outlier Removes points that are further away from their neighbors in average.",
        "open3d.visualization.VisualizerWithEditing.remove_geometry Function to remove geometry",
        "open3d.t.geometry.PointCloud.remove_statistical_outliers Remove points that are further away from their p nb_neighbor\nneighbors in average. This function is not recommended to use on GPU.",
        "open3d.visualization.VisualizerWithKeyCallback.remove_geometry Function to remove geometry",
        "open3d.ml.tf.datasets.utils.DataProcessing.remove_outside_points Remove points which are outside of image.",
        "open3d.visualization.VisualizerWithVertexSelection.remove_geometry Function to remove geometry",
        "open3d.ml.torch.datasets.utils.DataProcessing.remove_outside_points Remove points which are outside of image.",
        "void cv::detail::tracking::TrackerContribFeatureSet::removeOutliers Remove outliers for all feature types (optional)",
        "open3d.t.geometry.PointCloud.remove_duplicated_points Remove duplicated points and there associated attributes.",
        "open3d.visualization.VisualizerWithVertexSelection.remove_picked_points Function to remove picked points"
    ],
    [
        "void  pcl::SACSegmentation <  PointT  >::segment Base method for segmentation of a model in a  PointCloud  given by <setInputCloud (), setIndices ()>\n\nDefinition at line  80  of file  sac_segmentation.hpp .\n\nReferences  pcl::ModelCoefficients::header ,  pcl::PointIndices::header ,  pcl::PointIndices::indices , and  pcl::ModelCoefficients::values .",
        "virtual void cv::hfs::HfsSegment::setMinRegionSizeII : set and get the parameter minRegionSizeII. This parameter is used in the third stage mentioned above. It serves the same purpose as minRegionSizeI",
        "using  pcl::SACSegmentation <  PointT  > ::PointCloud  =   pcl::PointCloud < PointT > Definition at line  73  of file  sac_segmentation.h .",
        "float cv::QRCodeDetectorAruco::Params::minModuleSizeInPyramid The minimum allowed pixel size of a QR module in the smallest image in the image pyramid, default 4.f.",
        "using  pcl::SACSegmentationFromNormals <  PointT , PointNT > ::PointCloud  =  typename  SACSegmentation < PointT > ::PointCloud Definition at line  319  of file  sac_segmentation.h .",
        "void  pcl::outofcore::OutofcoreOctreeBaseNode < ContainerT,  PointT  >::queryFrustum Warning This function is only available if the visualization module is available and the preprocessor symbol  PCL_VISUALIZATION_AVAILABLE  is defined.",
        "using  pcl::SACSegmentationFromNormals <  PointT , PointNT >::SampleConsensusModelPtr =  typename  SampleConsensusModel < PointT >::Ptr Definition at line  328  of file  sac_segmentation.h .",
        "float cv::QRCodeDetectorAruco::Params::maxModuleSizeMismatch The maximum allowed relative mismatch in module sizes for finder patterns in the same QR code, default 1.75f.",
        "SampleConsensusPtr   pcl::SACSegmentation <  PointT  >::sac_ {nullptr} The sample consensus segmentation method.\n\nDefinition at line  254  of file  sac_segmentation.h .\n\nReferenced by  pcl::SACSegmentation< PointT >::getMethod() .",
        "open3d.visualization.gui.Application.initialize Overloaded function.\n\ninitialize(self: open3d.visualization.gui.Application) -> None\n\nInitializes the application, using the resources included in the wheel. One of the initialize functions _must_ be called prior to using anything in the gui module\n\ninitialize(self: open3d.visualization.gui.Application, arg0: str) -> None\n\nInitializes the application with location of the resources provided by the caller. One of the initialize functions _must_ be called prior to using anything in the gui module",
        "using  pcl::SACSegmentationFromNormals <  PointT , PointNT >::PointCloudN =   pcl::PointCloud <PointNT> Definition at line  323  of file  sac_segmentation.h .",
        "bool cv::cuda::DeviceInfo::isCompatible Checks the CUDA module and device compatibility.\n\nThis function returns true if the CUDA module can be run on the specified device. Otherwise, it returns false .",
        "SampleConsensusModelPtr   pcl::SACSegmentation <  PointT  >::model_ {nullptr} The model that needs to be segmented.\n\nDefinition at line  251  of file  sac_segmentation.h .\n\nReferenced by  pcl::SACSegmentation< PointT >::getModel() .",
        "static bool cv::cuda::TargetArchs::builtWith The following method checks whether the module was built with the support of the given feature:"
    ],
    [
        "bool pcl::concatenate #include <pcl/common/io.h>\n\nConcatenate two  pcl::PCLPointCloud2 .\n\nDefinition at line  322  of file  io.h .\n\nReferences  pcl::PCLPointCloud2::concatenate() .",
        "void pcl::device::ProbabilityProc::CUDA_SelectLabel This will merge the votes from the different trees into one final vote, including probabilistic's.",
        "void pcl::device::kinfuLS::mergePointNormal Merges  pcl::PointXYZ  and  pcl::Normal  to  PointNormal .",
        "void pcl::gpu::people::ProbabilityProcessor::SelectLabel This will merge the votes from the different trees into one final vote, including probabilistic's.",
        "void pcl::device::mergePointNormal Merges  pcl::PointXYZ  and  pcl::Normal  to  PointNormal .",
        "open3d.t.pipelines.registration.TransformationEstimationForDopplerICP Class to estimate a transformation between two point clouds using color information",
        "bool pcl::concatenate #include <pcl/common/io.h>\n\nConcatenate two pcl::PointCloud<PointT>\n\nDefinition at line  304  of file  io.h .\n\nReferences  pcl::PointCloud< PointT >::concatenate() .\n\nReferenced by  pcl::PCLPointCloud2::concatenate() ,  pcl::PolygonMesh::concatenate() ,  pcl::outofcore::OutofcoreOctreeDiskContainer< PointT >::insertRange() ,  pcl::PolygonMesh::operator+=() ,  pcl::outofcore::OutofcoreOctreeBaseNode< ContainerT, PointT >::queryBBIncludes() ,  pcl::outofcore::OutofcoreOctreeBaseNode< ContainerT, PointT >::queryBBIncludes_subsample() , and  pcl::outofcore::OutofcoreOctreeDiskContainer< PointT >::read() .",
        "open3d.t.pipelines.registration.TransformationEstimationForColoredICP Class to estimate a transformation between two point clouds using color information",
        "PointCloud   pcl::PointCloud <  PointT  >::operator+ Add a point cloud to another cloud.\n\nDefinition at line  226  of file  point_cloud.h .",
        "open3d.pipelines.registration.TransformationEstimationForColoredICP Class to estimate a transformation between two point clouds using color information",
        "PCLPointCloud2  pcl::PCLPointCloud2::operator+ Add a point cloud to another cloud.\n\nDefinition at line  84  of file  PCLPointCloud2.h .",
        "Mat  cv::videostab::estimateGlobalMotionLeastSquares #include < opencv2/videostab/global_motion.hpp >\n\nEstimates best global motion between two 2D point clouds in the least-squares sense.",
        "PCLPointCloud2  & pcl::PCLPointCloud2::operator+= Add a point cloud to the current cloud.",
        "open3d.pipelines.registration.CorrespondenceCheckerBasedOnDistance.Check Function to check if two points can be aligned. The two input point clouds must have exact the same number of points."
    ],
    [
        "void pcl::MLSResult::computeMLSSurface Smooth a given point and its neighborhood using Moving Least Squares.\n\nDefinition at line  689  of file  mls.hpp .\n\nReferences  pcl::compute3DCentroid() ,  pcl::computeCovarianceMatrix() , and  pcl::eigen33() .\n\nReferenced by  pcl::MovingLeastSquares< PointInT, PointOutT >::computeMLSPointNormal() .",
        "void  pcl::MovingLeastSquares < PointInT, PointOutT >::computeMLSPointNormal Smooth a given point and its neighborghood using Moving Least Squares.\n\nDefinition at line  173  of file  mls.hpp .\n\nReferences  pcl::MLSResult::computeMLSSurface() ,  pcl::MLSResult::curvature ,  pcl::MLSResult::MLSProjectionResults::normal ,  pcl::MLSResult::num_neighbors ,  pcl::MLSResult::MLSProjectionResults::point ,  pcl::MLSResult::projectPointSimpleToPolynomialSurface() ,  pcl::MLSResult::projectPointToMLSPlane() , and  pcl::MLSResult::projectQueryPoint() .",
        "pcl::MovingLeastSquaresOMP < PointInT, PointOutT >:: MovingLeastSquaresOMP Constructor for parallelized Moving Least Squares.\n\nDefinition at line  758  of file  mls.h .",
        "pcl::MLSResult::MLSProjectionResults::MLSProjectionResults ",
        "int cv::legacy::TrackerBoosting::Params::numClassifiers the number of classifiers to use in a OnlineBoosting algorithm",
        "std::vector< MLSResult >  pcl::MovingLeastSquares < PointInT, PointOutT >::mls_results_ {} Stores the MLS result for each point in the input cloud.\n\nDefinition at line  555  of file  mls.h .\n\nReferenced by  pcl::MovingLeastSquares< PointInT, PointOutT >::getMLSResults() .",
        "enum  cv::ximgproc::HoughDeskewOption Specifies to do or not to do skewing of Hough transform image.\n\nThe enum specifies to do or not to do skewing of Hough transform image so it would be no cycling in Hough transform image through borders of image.",
        "void  pcl::MovingLeastSquares < PointInT, PointOutT >::performProcessing Abstract surface reconstruction method.\n\nImplements  pcl::CloudSurfaceProcessing< PointInT, PointOutT > .\n\nDefinition at line  283  of file  mls.hpp .\n\nReferences  pcl::PointCloud< PointT >::begin() ,  pcl::PointCloud< PointT >::end() ,  pcl::PointCloud< PointT >::insert() , and  pcl::PointCloud< PointT >::size() .",
        "using  pcl::MovingLeastSquares < PointInT, PointOutT >::Ptr =  shared_ptr< MovingLeastSquares <PointInT, PointOutT> > Definition at line  256  of file  mls.h .",
        "enum  cv::GrabCutClasses #include < opencv2/imgproc.hpp >\n\nclass of the pixel in GrabCut algorithm",
        "using  pcl::MovingLeastSquares < PointInT, PointOutT >::PointCloudIn =   pcl::PointCloud <PointInT> Definition at line  274  of file  mls.h .",
        "void pcl::gpu::people::RDFBodyPartsDetector::process This function does the complete RDF evaluation in a discrete manner and builds the blob matrix."
    ],
    [
        "using  pcl::Feature < PointInT, PointOutT >::KdTree =   pcl::search::Search <PointInT> Definition at line  117  of file  feature.h .",
        "open3d.geometry.KDTreeFlann KDTree with FLANN for nearest neighbor search.",
        "KdTreePtr   pcl::MinCutSegmentation <  PointT  >::search_ {nullptr} Stores the search method that will be used for finding K nearest neighbors.\n\nNeighbours are used for building the graph.\n\nDefinition at line  282  of file  min_cut_segmentation.h .",
        "open3d.geometry.KDTreeFlann.set_matrix_data Sets the data for the KDTree from a matrix.",
        "using  pcl::MovingLeastSquares < PointInT, PointOutT >::KdTree =   pcl::search::Search <PointInT> Definition at line  265  of file  mls.h .",
        "virtual int cv::ml::KNearest::getEmax Parameter for KDTree implementation.",
        "using  pcl::GrabCut <  PointT  >::KdTree =   pcl::search::Search < PointT > Definition at line  320  of file  grabcut_segmentation.h .",
        "open3d.geometry.KDTreeSearchParam Base class for KDTree search parameters.",
        "using  pcl::search::KdTree <  PointT ,  Tree  > ::PointCloud  =  typename  Search < PointT > ::PointCloud Definition at line  64  of file  kdtree.h .",
        "open3d.geometry.KDTreeSearchParamKNN KDTree search parameters for pure KNN search.",
        "using  pcl::PCLSurfaceBase < PointInT >::KdTree =   pcl::search::Search <PointInT> Definition at line  65  of file  reconstruction.h .",
        "open3d.geometry.KDTreeSearchParamRadius KDTree search parameters for pure radius search.",
        "using  pcl::EuclideanClusterExtraction <  PointT  >::KdTree =   pcl::search::Search < PointT > Definition at line  336  of file  extract_clusters.h .",
        "open3d.visualization.gui.ImageWidget.ui_image Replaces the texture with a new texture. This is not a fast path, and is not recommended for video as you will exhaust internal texture resources."
    ],
    [
        "pcl::NarfDescriptor::NarfDescriptor Constructor.",
        "static void pcl::Narf::extractForEveryRangeImagePointAndAddToList Extract an NARF for every point in the range image.",
        "float* pcl::Narf::descriptor_ {nullptr} Definition at line  284  of file  narf.h .\n\nReferenced by  copyToNarf36() , and  getDescriptorDistance() .",
        "double  pcl::MarchingCubesRBF < PointNT >::kernel the Radial Basis Function kernel.\n\nDefinition at line  108  of file  marching_cubes_rbf.hpp .",
        "pcl::NarfDescriptor::Parameters::Parameters ",
        "virtual void cv::hfs::HfsSegment::setMinRegionSizeII : set and get the parameter minRegionSizeII. This parameter is used in the third stage mentioned above. It serves the same purpose as minRegionSizeI",
        "bool pcl::Narf::extractFromRangeImage Method to extract a NARF feature from a certain 3D point using a range image.\n\npose determines the coordinate system of the feature, whereas it transforms a point from the world into the feature system. This means the interest point at which the feature is extracted will be the inverse application of pose onto (0,0,0). descriptor_size_ determines the size of the descriptor, support_size determines the support size of the feature, meaning the size in the world it covers",
        "virtual void cv::bioinspired::TransientAreasSegmentationModule::getSegmentationPicture access function return the last segmentation result: a boolean picture which is resampled between 0 and 255 for a display purpose",
        "pcl::Narf::Narf Constructor.",
        "int pcl::Narf::getNoOfBeamPoints How many points on each beam of the gradient star are used to calculate the descriptor?\n\nDefinition at line  151  of file  narf.h .\n\nReferences  pcl_lrint .",
        "pcl::NarfKeypoint::NarfKeypoint ",
        "anonymous enum #include < opencv2/stereo.hpp >\n\nspeckle removal algorithms. These algorithms have the purpose of removing small regions",
        "void pcl::NarfDescriptor::computeFeature Implementation of abstract derived function.\n\nImplements  pcl::Feature< PointWithRange, Narf36 > .",
        "virtual void cv::hfs::HfsSegment::setSegEgbThresholdII : set and get the parameter segEgbThresholdII. This parameter is used in the third stage mentioned above. It serves the same purpose as segEgbThresholdI. The segmentation result tends to have more regions remained if this value is large and vice versa."
    ]
]
}